{
  "api": "1",
  "name": "Example Scripts Demo",
  "slug": "scripts-demo",
  "version": "0.3.2",
  "description": "Ships a JavaScript file that pins a banner to the bottom of the viewer page (and logs to the console) so an admin can see at a glance that the plugin's script loaded, with no dependency on a live stream or chat. This example was written in Python.",
  "category": "examples",
  "permissions": [
    "ui.modify",
    "http.serve"
  ],
  "scripts": [
    "client.js"
  ]
}
