# hello-world

The minimum viable plugin. No event handlers, just enough to verify that the load + `register()` validation path works end-to-end. A plugin author can start here and add handlers one at a time.

**Demonstrates:** manifest parsing, `definePlugin({})` with zero handlers, the host's load + register handshake.
