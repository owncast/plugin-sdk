# echo-bot

Replies to every chat message with `"<user> said: <body>"`. Posts under the plugin's own bot identity that the host provisions automatically.

**Demonstrates:** posting to chat via `owncast.chat.send(text)`, the `chat.send` permission, the per-plugin bot identity model.
